const Loading: React.FC = () => {
    return (
        <div className="flex items-center bg-darkVoid text-blancoHueso justify-center h-screen">
            <p>Loading...</p>
        </div>
    );
};

export default Loading;
